export default class Product {
  constructor(id, name, description, imageSrc, price, tags) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.imageSrc = imageSrc;
    this.price = price;
    this.tags = tags;
  }
}
